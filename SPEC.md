# オシャボ管理アプリ 仕様書

- リポジトリ: `Hiroki-Ko/specialPokeBalls`
- 作成日: 2026-08-15
- ステータス: ドラフト(レビュー反映済み・最終確認待ち)

## 1. 概要

### 1.1 背景・目的

ポケモンには「オシャボ(オシャレボール)」という文化がある。「このポケモンはこのボールに入れたい」という組み合わせを決めて集める楽しみ方や、ゲーム内で入手方法が限られているレアボールに入れるコレクター的な要素もある。

これまで欲しい組み合わせや入手状況を頭の中やメモ帳で管理していたが、件数が増えると管理が煩雑になる。本プロジェクトでは、オシャボの「入れられる組み合わせリスト」と「入手済みステータス」をWebアプリ上で一元管理できるようにすることを目的とする。

### 1.2 用語definition

| 用語 | 説明 |
|---|---|
| オシャボ / オシャレボール | 特定のポケモンに合わせて選ぶ、こだわりのモンスターボール |
| 未入手 | ポケモン×オシャボの組み合わせを「欲しいリスト」に登録したが、まだ実現していない状態 |
| 入手済み | 実際にそのポケモン×ボールの組み合わせを捕獲・厳選・交換などで実現した状態 |
| ポケモンマスタ | 全国No.・ポケモン名・フォルムなど、ポケモン自体の情報を持つ参照専用データ(6.1・6.2参照) |
| 収録対象 | ポケモンマスタに含めるポケモンの範囲。進化ライン(無進化を含む)の最初の姿のみを対象とし、リージョンフォームはそれぞれ独立した収録対象として扱う(6.2.1参照) |

## 2. 対象ユーザー・利用形態

- 利用者: 本人(Hirokiさん)のみを想定した個人管理ツール
- ログイン・認証機能は不要
- マルチデバイス同期は現時点では必須要件としない(将来の検討事項とする)

## 3. 機能要件

### 3.1 ポケモンの登録

新しいゲームタイトルが発売されるたびに、100匹以上のポケモンが一度に追加対象となる運用を想定している。そのため、1匹ずつ入力する個別登録に加えて、まとめて登録できる一括登録の両方を用意する。ポケモン自体の情報(全国No.・ポケモン名・フォルム)は6.1のポケモンマスタから参照するため、登録時にあらためて入力する必要はない。

#### 3.1.1 個別登録

以下の項目を入力・選択し、ポケモン1件をオシャボリストに登録できる。登録時、3.1.3の対象ボール一覧がすべて「未入手」の状態でそのポケモンに紐づく。

- 対象ポケモン(必須。全国No.またはポケモン名で検索し、ポケモンマスタから選択する。同じ全国No.に複数のフォルムが存在する場合はフォルムもあわせて選択する)。選択すると、そのポケモンの夢特性(ポケモンマスタの情報)が自動的に表示される(入力不要)
- メモ(任意。厳選個体値、色違い希望などの補足)

#### 3.1.2 一括登録

新タイトル発売時など、多数のポケモンをまとめて登録するための機能。

- テキストエリアに1行1件の形式で複数貼り付け、まとめて登録できる(初期案。CSVアップロード形式にする案もあるが、貼り付けの方が手軽なため一旦こちらを採用。使いにくければ後で見直す)
- 1行の形式は「全国No.,フォルム」とし、全国No.のみ必須。全国No.はポケモンマスタと照合し、該当するポケモン名・夢特性は自動的に補完されるため入力不要。フォルムを省略した場合はその全国No.の基本フォルムとして登録される(例: `906`)
- メモは一括登録時点では空欄とし、必要な場合は登録後に3.4の編集機能で個別に追記する
- 指定した全国No.(・フォルム)がポケモンマスタに存在しない場合はその行の登録をスキップし、結果画面でスキップした行を一覧表示する(入力ミスに気づけるようにする)
- 一括登録された各ポケモンにも、3.1.3の対象ボール一覧の入手状況(すべて「未入手」)が個別登録と同様に自動的に紐づく

#### 3.1.3 管理対象ボール一覧(オシャボ一覧)

以下のボールを管理対象とし、登録した1ポケモンに対してそれぞれ入手状況を紐づける。この一覧は運用上ほぼ増減しない想定のため、固定のマスタ一覧として扱う。

- ラブラブボール
- ムーンボール
- ヘビーボール
- レベルボール
- フレンドボール
- スピードボール
- ルアーボール
- サファリボール
- コンペボール
- ドリームボール
- ウルトラボール

### 3.2 一覧表示・検索・並び替え

- 登録済みのポケモンを一覧表示する(表示名・全国No.・画像などはポケモンマスタと結合して表示する)
- ステータス(未入手/入手済み)で絞り込みできる
- ゲームタイトルで絞り込みできる。タイトルを選択すると、そのタイトルに登場(内定)するポケモン(ポケモンマスタのgameTitlesに選択したタイトルが含まれるもの、および6.2.2の手動登録分)のみが表示される。選択肢はポケモンマスタのgameTitlesおよび手動登録分に実際に存在するタイトルのみとする
- ポケモン名・全国No.・ボール名でキーワード検索できる(部分一致)
- 一覧テーブルの「No.」または「ポケモン」の見出しをクリックすると並び替えができる。クリックのたびに デフォルト(登録順)→昇順→降順→デフォルト の順で切り替わる

### 3.3 ステータス管理

- 各ポケモンについて、3.1.3の対象ボールごとに「未入手」⇔「入手済み」を切り替えられる

### 3.4 編集・削除

- 登録済みのオシャボリストの内容(対象ポケモン・フォルムの選び直し、メモ)を編集できる。主な用途は登録ミスの修正や、一括登録時に空欄にした項目の追記
- 不要になったポケモンの登録(二重登録など)を削除できる(確認ダイアログを表示する)

### 3.5 データのエクスポート/インポート

- 登録データをJSON形式でエクスポートできる(バックアップ・他端末への移行用)
- エクスポートしたJSONを再度インポートできる

## 4. 非機能要件

- 個人利用が前提のため、サーバーサイドの認証・DB基盤は必須としない
- PC・スマホ双方のブラウザで問題なく閲覧・操作できることが望ましい(レスポンシブ対応)
- 追加コストが発生しない構成(無料枠の範囲)で運用できること
- ポケモンマスタは静的データとしてアプリに同梱し、アプリの通常利用時に外部APIへアクセスする必要がないこと

## 5. 画面構成(案)

1. **一覧画面(ホーム)**
   - ポケモンごとの一覧(各行に画像・全国No.・ポケモン名・3.1.3のオシャボ一覧の入手状況をまとめて表示。画像・ポケモン名・全国No.はポケモンマスタから参照)
   - テーブル見出し(No./ポケモン)は画面上部に固定表示され、スクロールしても常に見える
   - 上部にステータスフィルタ・ゲームタイトルフィルタ・検索ボックス(部分一致)
   - 「新規登録」「一括登録」「ゲームタイトル手動登録」「エクスポート」「インポート」の各操作は、絞り込み欄右端の「編集」ボタンで開閉するドロワー状のパネルにまとめて配置する(常時表示はしない)
2. **個別登録フォーム**
   - ポケモン名または全国No.での検索・選択(オートコンプリート)、フォルム選択を行うと夢特性が自動表示される。あわせてメモを入力する(3.1.1)
3. **一括登録フォーム**
   - 全国No.範囲を指定してポケモンマスタから対象をまとめて抽出する方式と、テキストエリアへの「全国No.,フォルム」形式での貼り付けによる方式の2通り(3.1.2)
4. **詳細表示**
   - 一覧の各ポケモンをクリックすると、そのポケモンのオシャボごとのステータスを確認・切り替えできるほか、編集・削除が行える(3.4)
   - あわせて、そのポケモンの夢特性・タマゴグループ・孵化までの必要歩数(いずれもポケモンマスタの情報)を、見出しと値の2カラムの表形式で確認できる
5. **ゲームタイトル手動登録画面**
   - PokéAPI側のロケーションエリア出現データが第7世代以降で不足していることを補うため、①タイトルを選択(または新規入力)→②そのタイトルに内定しているポケモンを選択、の手順で内定情報を手動登録できる(6.2.2参照)。登録内容はポケモンマスタ本体を書き換えず、ブラウザ側に追加情報として保持し、一覧の絞り込み等ではマスタの情報と合算して扱う

## 6. データ設計(案)

データは、ポケモン自体の情報を持つ「ポケモンマスタ」と、ユーザーが管理するオシャボの登録内容を持つ「オシャボエントリ」に分ける。オシャボエントリ側はポケモンマスタをIDで参照するのみとし、名前や全国No.を重複して持たない。

### PokemonMaster(ポケモンマスタ)※参照専用・アプリ内では編集しない

| フィールド | 型 | 説明 |
|---|---|---|
| id | string | ポケモン(フォルム込み)を一意に識別するID |
| nationalNo | number | 全国No.(図鑑番号) |
| name | string | ポケモン名 |
| formName | string \| null | フォルム名(基本フォルムはnull。リージョンフォームの場合は「アローラ」「ガラル」等) |
| eggGroups | string[] | タマゴグループ(複数の場合あり) |
| hatchSteps | number | 孵化までの必要歩数 |
| hiddenAbility | string \| null | 夢特性(そのポケモンに夢特性が存在しない場合はnull) |
| gameTitles | string[] | そのポケモンが登場する(=捕獲できる)ゲームタイトル一覧 |
| spriteId | number | 画像表示用のPokéAPI内部ID(一覧のポケモン画像取得に使用。フォルム違いは全国No.と別の値を持つ) |

このマスタの収録範囲・用意方法は6.2を参照。タマゴグループ・孵化までの必要歩数・夢特性は一覧に常時表示するものではないが、詳細表示(5章)で確認できるようにする。画像(spriteId)は一覧に常時表示する。

### OshaboEntry(オシャボリストのユーザー登録データ、旧PokemonRecord)

| フィールド | 型 | 説明 |
|---|---|---|
| id | string | 一意なID(UUID等) |
| pokemonId | string | 対象ポケモン(PokemonMaster.idへの参照) |
| note | string \| null | メモ |
| createdAt | string (ISO日時) | 登録日時 |
| ballStatuses | BallStatus[] | 3.1.3の各ボールの入手状況(下記) |

### BallStatus(オシャボごとの入手状況)

| フィールド | 型 | 説明 |
|---|---|---|
| ballType | string | ボールの種類(3.1.3の一覧から) |
| status | "未入手" \| "入手済み" | ステータス |

#### 6.1 補足

- ボールの種類(3.1.3)は運用上ほぼ増減しない想定のため、当面固定のマスタ一覧として扱い、オシャボエントリ登録時にこの一覧分のBallStatusがまとめて生成される
- 夢特性はポケモン種族に対して1つ決まる情報のため、オシャボエントリ側では持たずポケモンマスタ側のhiddenAbilityを参照する(名前や全国No.と同様、重複して持たない)
- ゲームタイトル(gameTitles)はポケモンマスタ側のみが持つ参照データであり、オシャボエントリ側では持たない。3.2の一覧画面でゲームタイトルによる絞り込みを行う際に、選択したタイトルがポケモンマスタのgameTitlesに含まれるポケモンだけを表示する、という使い方をする。件数が多くないため、別途DBテーブルは設けず、当面はコード内の定数リストとして保持すれば十分と考える

#### 6.2 ポケモンマスタデータの用意方法

ポケモンマスタ(全国No.・ポケモン名・フォルム・タマゴグループ・孵化までの必要歩数・夢特性・登場ゲームタイトル)は、以下を参考に外部データから生成する。

#### 6.2.1 収録範囲

- 収録対象は「進化ライン(無進化を含む)の最初の姿」のみとする(例: フシギダネは収録するが、進化先のフシギソウ・フシギバナは収録しない)。PokéAPIの進化チェーン情報(他の種から進化しない、すなわち`evolves_from_species`を持たない種)を基準に判定する
- リージョンフォーム(アローラのすがた、ガラルのすがた等)は、それぞれ独立したポケモンとして収録する(例: ニャース、ニャース(アローラ)、ニャース(ガラル)を別々のレコードとして収録)。各リージョンフォームについても、上記と同様に進化ラインの最初の姿のみを対象とする
- メガシンカ・ゲンシカイキなど、収録対象外としたい姿は、参考記事同様に名前の末尾パターン(例: "-mega"、"-primal")等で判別して除外する
- タマゴグループが「タマゴみはっけん」のみ、かつ進化ライン自体を持たない(誰にも進化しない)種は、タマゴ・孵化にまつわる収集要素と無縁のため収録対象から除外する(伝説・幻のポケモン等を想定)。タマゴみはっけんであっても進化する種(例: コイキング)は除外しない

#### 6.2.2 データソース・生成方法

- 参考記事: 「ポケモン対戦画面読み上げアプリのデータ管理について」(takosavi氏のブログ、2024/12/30) https://takosavi.hatenablog.com/entry/2024/12/30/001209
- 参考実装: GitHubリポジトリ `takosavi/pokescreader-database`
- データソースには **PokéAPI**(ポケモンのあらゆる情報を取得できるオープンソースのWeb APIサービス)を利用する
- PokéAPIは「種(Species)」「個体(Pokémon)」「姿(Form)」の3階層構造を持つため、`/pokedex/national` エンドポイントなどから全国図鑑情報を取得し、各階層の参照フィールドを辿って全国No.・ポケモン名・フォルム名を抽出する
- タマゴグループはSpeciesデータの`egg_groups`から、孵化までの必要歩数はSpeciesデータの`hatch_counter`から算出して取得する
- 夢特性はPokémonデータの`abilities`のうち`is_hidden`がtrueのものから取得する(存在しない場合はnull)
- 登場ゲームタイトル(gameTitles)は、バージョン別の野生出現情報(`/pokemon/{id}/encounters`など)をもとに判定する。配布(イベント配布など)でのみ入手可能で野生では出現しないバージョンは、登場(内定)しているとはみなさず、gameTitlesに含めない
- コロシアム・XD・スタジアム等、本編と直接関係しない外伝・周辺タイトルのバージョンは、PokéAPI上に存在してもgameTitlesの収録対象外とする
- gameTitlesはタイトルの発売順に並べて格納する
- 画像表示用に、PokéAPI内部のポケモンID(`spriteId`。フォルム違いは全国No.と別の値を持つ)もあわせて保存する
- 生成したデータはJSON等の静的ファイルとしてリポジトリに保存し、アプリはビルド時・起動時にこの静的データを読み込む(4章の通り、アプリの通常利用時にPokéAPIへ実行時アクセスする必要はない)
- 新タイトル発売時など、対象ポケモンの追加が必要になったタイミングで、このデータ生成処理を再実行してマスタを更新する運用とする
- **既知の制約**: PokéAPI自体が第7世代(サン・ムーン)以降のロケーションエリア出現データを十分に持っていない(コミュニティでも既知の欠落。参考: https://github.com/PokeAPI/pokeapi/discussions/958 )。そのためLet's Go/ソード・シールド/BDSP/Legendsシリーズ/スカーレット・バイオレット等では、実際に野生出現するにも関わらずgameTitlesに載らないポケモンが一定数発生し得る。この制約は6.2.3の手動登録機能で補う

#### 6.2.3 ゲームタイトルの手動補完(アプリ内機能)

6.2.2記載の制約により自動取得できないゲームタイトルの内定情報を、アプリ内の「ゲームタイトル手動登録」画面から手動で補完できるようにする。

- 手順: ①タイトルを選択(既存の一覧から選ぶか、新しいタイトル名を自由入力する)→②そのタイトルに内定していると判断したポケモンを一覧から選択(全国No.範囲でまとめて選択も可)→③登録する
- ポケモンマスタ側で既にgameTitlesに含まれているものは、変更不可(常にチェック済み)として扱い、手動登録の対象は自動取得できていない分のみとする
- 登録内容はポケモンマスタ本体(pokemonMaster.json)を書き換えるのではなく、ブラウザのlocalStorageに「タイトル名 → 手動で内定と判断したポケモンIDの配列」として保存する
- 一覧画面の絞り込み等では、ポケモンマスタのgameTitlesとこの手動登録分を合算した内容で判定する

## 7. 技術スタック(案)

個人利用のシンプルなWebアプリであることを踏まえ、以下を初期案とする。

- **フロントエンド**: React + Vite + TypeScript
- **スタイリング**: Tailwind CSS(任意。素のCSSでも可)
- **状態管理**: React標準のuseState/useReducerで十分な規模と想定
- **データ永続化**: ブラウザのlocalStorageに保存(サーバー不要で個人利用に十分)。3.5のエクスポート/インポート機能でバックアップ・移行に対応
- **ポケモンマスタデータ生成**: Node.jsスクリプトでPokéAPIから取得・整形し、静的JSONとしてリポジトリに保存(6.2参照)。アプリ本体はこの静的データのみを参照する
- **ホスティング**: GitHub Pages(静的サイトとして無料で公開・自分だけがURLを知っていればよい)

上記はあくまで初期案であり、実装フェーズで見直しても良い。

## 8. 開発の進め方(プロジェクトフロー)

1. 要件定義(完了)
2. 仕様書作成 ← 本ドキュメント
3. 仕様書レビュー・確定
4. 画面設計・データ設計の詳細化(必要であれば簡易ワイヤーフレーム作成)
5. 開発環境構築(Vite等のセットアップ、リポジトリへの初期コミット)
6. ポケモンマスタデータの生成(6.2の方法でPokéAPIから取得・整形し、静的データを用意)
7. 実装(MVP: 個別登録・一括登録・一覧・ステータス管理・編集削除を実装し、エクスポート/インポートは後続で追加)
8. 動作確認
9. GitHub Pagesへのデプロイ
10. 追加機能の検討・改善(画像添付、入手経緯の詳細記録など)

## 9. 今後の検討事項(Open Questions)

- ポケモンマスタの生成対象範囲(全世代分を最初から用意するか、必要な世代分だけ順次追加していくか)
- 一括登録の入力方式(テキストエリア貼り付け vs CSVアップロード)は貼り付け方式を初期案としているが、実際に100匹規模を登録する際に使いやすいか、実装後に確認したい
- 画像(捕獲時のスクリーンショット等)の添付は将来的に対応するか
- 入手経緯(自己捕獲/厳選/交換など)の詳細記録は必要か
- 複数端末での利用やクラウド同期が将来的に必要になった場合の対応方針
