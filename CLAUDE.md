# Claude Code 向け: 作業開始前に必ず読むこと

このリポジトリで作業する(コードを読む・修正する・提案する)前に、必ず **[`PROJECT_LOG.md`](./PROJECT_LOG.md)** を読んでください。

## なぜこのファイルがあるか

このプロジェクトは複数回のAIセッション(会話)にまたがって開発されています。セッションが切れる・コンテキストが失われる等の理由で、それ以前の会話で決定した設計判断や経緯が引き継がれないことが実際に何度か発生しました。特に「マスタ全種を常に一覧表示する」という仕様への変更は、一度その経緯(なぜそうしたか)が失われかけたことがあります。

`PROJECT_LOG.md` は、そうした設計判断・経緯・トラブルシューティング履歴を人間(Hiroki)とAIの両方が参照できる形でまとめた、このリポジトリの「記憶」です。`SPEC.md` が機能仕様そのものを定義するのに対し、`PROJECT_LOG.md` は「なぜ今の形になっているか」「過去に何を直したか」を記録します。

## 作業時の約束

1. 作業を始める前に `PROJECT_LOG.md` を読み、これまでの経緯(特に「設計判断の経緯」セクション)を把握してください。
2. 仕様の解釈に迷った場合、コードコメントだけでなく `PROJECT_LOG.md` も確認してください。
3. 重要な仕様判断・設計変更・トラブルの原因究明を行った場合は、作業の最後に `PROJECT_LOG.md` に追記してください(セクションの末尾に追記する形で構いません。過去の記述を消さないこと)。
4. このファイル(`CLAUDE.md`)自体はあくまで `PROJECT_LOG.md` へ誘導するための短いポインタです。詳細はすべて `PROJECT_LOG.md` 側に書いてください。

## 認証について

このアプリのAPIには意図的に認証・アクセス制御を実装していません(ユーザー本人の明示的な判断)。指示がない限り認証機能を追加しないでください。
