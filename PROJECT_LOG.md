# PROJECT_LOG.md — オシャボ管理 開発経緯・意思決定ログ

## このファイルについて

このプロジェクトの開発は複数回のAIとの会話セッションにまたがっており、セッションが「コンテキスト切れ」で要約に置き換わることがある。要約は大まかな流れは残るが、細かい設計判断の理由や経緯が失われることがあり、実際にそれが原因で仕様の行き違いが発生したことがある(詳細は9章参照)。

**次にこのプロジェクトの作業を引き継ぐAIアシスタント(Cowork・Claude Codeのどちらのセッションであっても)は、作業を始める前に必ずこのファイルを通読すること。** 特に4章「マスタ全種デフォルト表示の設計」と5章「ゲームタイトル関連の仕様」は、会話の記憶だけでは復元しにくい重要な仕様なので要注意。

新しく重要な仕様判断・変更を行った場合は、都度このファイルに追記すること(該当セクションに追記するか、末尾に新しいセクションを追加する)。読むだけでなく更新も忘れずに。

## 1. アプリ概要

- 個人用ポケモン「オシャボ」(特別なボール)収集管理アプリ。
- スタック: Vite + React + TypeScript + Tailwind CSS。バックエンドはCloudflare Pages Functions + D1(SQLite互換)。
- リポジトリ: `Hiroki-Ko/specialPokeBalls`。ローカルの作業場所: `C:\Users\User\my_apps\specialPokeBalls`。
- 本番URL: `https://specialpokeballs.pages.dev`。
- 詳しい機能仕様(ボール一覧・データ設計・マスタ生成ルール等)は `SPEC.md` を参照。このファイルはSPEC.mdを補うもので、「何が・なぜそうなったか」という経緯・意思決定を記録する場所。

## 2. アーキテクチャ上の主要決定

- 当初はlocalStorage保存 + GitHub Pages配信の構成だったが、スマホからのアクセス・複数端末でのデータ管理を実現するため、Cloudflare D1(+ Pages Functions)へ全面移行した。GitHub Pages用の`.github/workflows/deploy.yml`は移行前の名残で、現在は不要(削除して構わない)。
- 認証・アクセス制御は意図的に付けていない。ユーザーの明示判断:「保護なしでよい(URLを知らなければ見つからない前提)」。**今後、明示的な要望がない限りAPIに認証を追加しないこと。**
- ポケモンマスタ(図鑑データ)自体もD1の`pokemon_master`テーブルに保存している(当初はJSとして静的バンドルする案だったが、フルデータでの運用・更新のしやすさのためD1に移行)。

## 3. データモデル

- `entries`: ユーザーが明示登録したオシャボ収集エントリ(`id`, `pokemon_id`, `note`, `created_at`)。
- `ball_statuses`: エントリごとのボール入手状況(`entry_id`, `ball_type`, `status`)。
- `title_overrides`: PokéAPIの出現データ欠落を補うための手動タイトル登録(`title` -> `pokemonId[]`)。
- `pokemon_master`: 図鑑データ本体(`id`, `national_no`, `name`, `form_name`, `egg_groups`, `hatch_steps`, `hidden_ability`, `game_titles`, `sprite_id`)。
- `entries.pokemon_id`にUNIQUE制約あり(1ポケモンにつき実エントリは最大1件、という前提を守るため)。

## 4. 「マスタ全種デフォルト表示」の設計(重要な仕様転換)

- 元々は「新規登録」「一括登録」で明示的に登録したポケモンのみが一覧に表示される仕様だった(SPEC.mdの当初の設計に沿ったもの)。
- あるとき、ユーザーから「ローカル開発時には登録の有無に関わらず全て未入手の状態でマスタ情報を表示するようにしていたはずだが、なぜ変更したのか」という指摘があった。この時点のAI(Claude)側にはその変更を意図的に行った記憶がなく、原因はセッションのコンテキスト消失(要約への置き換え)による情報欠落だったと判明した。SPEC.md自体の記述と、実際にユーザーがローカルで動かしていたバージョンとの間に食い違いがあった可能性がある。
- **最終決定(現在の仕様)**: マスタ全件(現在446件、今後増える見込み)を常にデフォルトで一覧表示し、未操作のポケモンは「全ボール未入手・メモなし」の仮想行として扱う。実際にボール状態やメモを操作した瞬間に初めてD1へ実エントリを作成する(遅延作成)。
- 「削除」の意味もこれに伴い変更した: 行自体は一覧から消えず(マスタにある限り常に表示され続ける)、実エントリを削除して初期状態(全ボール未入手・メモなし)にリセットする、という意味になっている。
- 詳細画面の「対象ポケモンを変更する」機能は削除した(1行=1匹の特定のマスタポケモンに固定される仕様になったため、差し替えという操作自体が意味を持たなくなった)。この副作用として、フォーム違いのポケモン(例: 通常のすがた/アローラのすがた)に誤って入手状況を入力してしまった場合、以前のように「正しい方へ付け替える」ことができなくなった。手動でリセット→正しい行に再入力、という対応になる。ユーザーに説明済みで、現状はこの挙動で問題ないと了承済み。
- 「新規登録」「一括登録」ボタンはUI上に残しているが、全種デフォルト表示の仕様上、厳密には「一覧に追加する」という役割は不要になっている。検索してすぐメモを付けたい場合の簡易アクセス手段として残置している(将来的に不要と判断されれば削除してよい)。

## 5. ゲームタイトル関連の仕様

- PokéAPI自体が、第7世代(サン・ムーン)以降のソフトの野生出現データを十分に持っていない、という既知の制約がある(`scripts/generate-pokemon-master.mjs`のコメントにも記載)。これを補うのが「ゲームタイトル手動登録」機能であり、ソード・シールド以降のタイトルで自動取得に漏れが出るのは仕様上想定内(バグではない)。
- 新ソフト発売時に想定しているワークフロー(ユーザー指定):
  1. 新しいソフトタイトルを登録する(「ゲームタイトル手動登録」画面の①で自由入力すればよい)。
  2. 新規に追加されたポケモン自体をマスタへ一括登録する(`node scripts/generate-pokemon-master.mjs`を再実行して`npm run d1:seed-master:remote`で反映)。
  3. そのソフトに出現するポケモンを一括選択・登録する(「ゲームタイトル手動登録」画面の②)。
  4. 絞り込みで該当ソフト(またはそのグループ)を選ぶと、登録されたポケモンだけが表示される。
  - 手順2(マスタの再生成・再投入)は`entries`・`ball_statuses`・`title_overrides`には一切影響しない。ポケモンの`id`はPokéAPI由来の安定したスラッグ文字列(例: `pikachu`, `sandshrew-alola`)であり、何度再生成しても同じ個体には同じidが振られるため、既存の入手状況・メモ・手動タイトル登録が消えたりズレたりすることはない(確認済み)。
- 世代ごとのグループ化(ユーザー指定の17グループ、`src/utils/gameTitleGroups.ts`の`GAME_TITLE_GROUPS`に実装済み): 赤緑青ピカ / 金銀クリスタル / ルビーサファイアエメラルド / ファイアレッド・リーフグリーン / ダイヤモンド・パール・プラチナ / ハートゴールド・ソウルシルバー / ブラック・ホワイト・ブラック2・ホワイト2 / X・Y / オメガルビー・アルファサファイア / サン・ムーン・ウルトラサン・ウルトラムーン / Let's Go!ピカチュウ・イーブイ / ソード・シールド / ブリリアントダイヤモンド・シャイニングパール / Legendsアルセウス / スカーレット・バイオレット / Legends ZA / ウィンド・ウェーブ。
  - 絞り込みドロップダウンは同世代をまとめて1つの選択肢にし、どれか1タイトルにでも出現すればヒットする(例:「ソード」にしか出現しなくても「ソード・シールド」を選べばヒットする)。該当ポケモンが0件のグループも(選んでも0件になるだけなので)問題ないとの判断で、常に全17グループを表示する。
  - 「ゲームタイトル手動登録」の①タイトル選択、および詳細画面の「出現するソフト」チップは、グループ化せず個別タイトル単位(ユーザー指定:「こちらはソフトごとに登録」)。実際に使われているかどうかに関わらず、既知の全個別タイトル(現在41件、`GAME_TITLE_RELEASE_ORDER`)を発売日の新しい順で常に表示する。該当しないものは単に未選択(または候補として出るだけ)なので問題ない、とユーザー了承済み。
  - 「ウィンド」「ウェーブ」はグループ定義・発売順リストには追加済みだが、PokéAPI側にまだこの2タイトルのバージョンslugが存在しないため、自動取得(`generate-pokemon-master.mjs`によるgameTitles欄への反映)の対象には**まだ**なっていない。発売後、PokéAPI側にデータが追加された時点で自動的に拾われるようになる想定。それまでは手動登録で対応する。
- 新しいタイトルを追加する際は、以下の3箇所を整合させて更新すること。
  1. `src/utils/gameTitleOrder.ts`(`GAME_TITLE_RELEASE_ORDER`、個別タイトルの発売順配列)
  2. `src/utils/gameTitleGroups.ts`(`GAME_TITLE_GROUPS`、グループ定義)
  3. `scripts/generate-pokemon-master.mjs`(`VERSION_JA`、PokéAPIのversion slugとの対応表。PokéAPI側にそのタイトルのslugが実在する場合のみ追加可能)

## 6. UI/UXの修正履歴(なぜそうなっているか)

- スマホでのアクセスを想定し、画面右下に100番台単位のジャンプボタン(▲▼)を実装した。「下」は常に次の100番台の先頭へ、「上」は現在のブロックの先頭でなければまずそのブロックの先頭へ、すでに先頭にいる場合のみ前のブロックへ(音楽プレーヤーの「前へ」ボタンと同じ挙動)。連続クリック時にスムーススクロールのアニメーションが終わる前に次のクリックが来ると位置がずれる問題は、直前にジャンプさせた先を記憶しておく方式(`lastJumpNoRef`等)で解消した。
- ひらがな入力でもカタカナ表記のポケモン名・ボール名にマッチするよう、`src/utils/kana.ts`で正規化(ひらがな→カタカナ変換)して比較するようにした。検索窓・「ゲームタイトル手動登録」の候補検索の両方に適用済み。
- 一覧テーブルの見出し(No./ポケモン/オシャボ進捗)が画面上部に固定表示されない不具合を修正した。原因はスタイルの問題ではなく、見出しの高さを測る`useLayoutEffect`の依存配列が`[]`だったこと。ローディング画面表示中(まだ`headerRef`を持つ要素がDOMに存在しない時)にこのeffectが1回だけ実行されて早期リターンしてしまい、読み込み完了後もheaderHeightが0のまま(=見出しがツールバーの裏に隠れる)になっていた。依存配列を`[loading]`に変更し、読み込み完了後の再描画時にも実行されるようにして解決した。
- `Modal.tsx`がスマホの小さい画面で本文が長くなると画面からはみ出し、保存ボタンが押せない・閉じるボタンも押せず画面をリロードしないと抜け出せない、という不具合を修正した。見出し(タイトル・閉じるボタン)を固定し、本文だけを縦スクロールさせる構造(`max-h-[90vh]` + `overflow-y-auto`)に変更した。
- 「ゲームタイトル手動登録」の候補一覧が50件上限で全国No.95程度までしか表示されなかった問題を解消(上限を撤廃)。あわせて全国No.順ではなくポケモン名の五十音順ソートに変更した。
- 詳細画面に「出現するソフト」の編集欄を追加した(緑=マスタ側で自動取得済み・変更不可、紫=手動登録済み、灰色=未登録。灰色をクリックで追加、紫をクリックで解除)。登録漏れ・間違いの修正用。著作権上の理由から、実際のゲームパッケージ画像は使わず、色分けされたバッジ(チップ)で表現している。

## 7. ローカル開発環境まわりの注意点

- `npm run dev`(素のVite)は`/api/*`のCloudflare Pages Functions・D1が動かないため、`fetch`がVite側のindex.htmlをそのまま返してJSONパースエラーになる。**実際の動作確認には必ず`npm run dev:full`を使うこと**(ビルド後、`wrangler pages dev`でFunctions・D1(ローカルエミュレーション)込みで起動する)。
- `wrangler pages dev`のデフォルトポート8788は他プロジェクト(例: `pocoapoke`)と衝突しやすいため、本プロジェクトでは明示的に**8799番ポート**を使うよう`package.json`の`dev:full`スクリプトに設定してある。`http://localhost:8799`を開くこと。
- ローカルD1とリモート(本番)D1は完全に別のデータベース。初回は`npm run d1:migrate:local`・`npm run d1:seed-master:local`を実行しないとローカルは空のまま(マスタもエントリも0件)。

## 8. マスタデータ生成・投入の安全装置

- `generate-pokemon-master.mjs`: PokéAPIへの個別リクエストの取得失敗が全体の5%を超えたら、`pokemonMaster.json`への書き込みを中止する(既存ファイルはそのまま残る)。意図的に不完全な結果でよい場合のみ`--allow-partial`で強制出力できる。
- `seed-pokemon-master.mjs`: 前回成功時の件数を`scripts/pokemon-master-seed-state.json`に記録しており、今回の件数がその90%を下回ったら中止する。範囲・除外条件を意図的に変更して件数が減った場合のみ`--force`で強制続行できる。このstateファイルは**gitで追跡する**(`.gitignore`に入れないこと)。
- どちらで中断した場合も、D1への反映コマンド自体(`wrangler d1 execute ...`)は実行されない(npm scriptsが`&&`で繋がっているため)。これは「取得失敗等で不完全なデータのまま本番マスタを上書きしてしまう」ことを防ぐための安全装置として、ユーザーからの明示的な要望で追加した。

## 9. セッション継続性についての注意(重要・経緯)

このプロジェクトは複数回、AIとの会話セッションが「コンテキスト切れ」で要約に置き換わっている。要約は大まかな流れは残るが、細かい設計判断の理由・経緯は失われることがある。実際に、4章に書いた「マスタ全種デフォルト表示」の経緯を巡って、ユーザーが以前ローカルで確認していた挙動と、AI側の記憶(要約後の状態)が食い違い、行き違いが発生したことがある。

このファイル(`PROJECT_LOG.md`)は、その反省を踏まえて作成した記録である。**次にこのプロジェクトの作業を引き継ぐAIアシスタント(Cowork・Claude Codeいずれのセッションであっても)は、作業を始める前に必ずこのファイルを通読すること。** 特に4章・5章は会話の記憶だけでは復元しにくいため要注意。

新しく重要な仕様判断・変更を行った場合は、都度このファイルに追記すること。
